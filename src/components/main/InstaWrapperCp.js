import React, { useEffect, useState } from 'react';
import styled from '../../style';
import withSnsWrapper from './withSnsWrapper';

const InstaWrapperCp = ({ list }) => {
  return <div></div>;
};

export default withSnsWrapper(InstaWrapperCp);
